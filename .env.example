PUID=1000
PGID=1000
TZ=Europe/Paris
DOMAIN=example.com
RADARR_PREFIX=radarr
SONARR_PREFIX=sonarr
QBITTORRENT_PREFIX=qbittorrent
PROWLARR_PREFIX=prowlarr
PLEX_PREFIX=plex
OVERSEERR_PREFIX=overseerr
PLEX_CLAIM=
TRAEFIK_NETWORK=traefik-net
PLEX_SHARED=/path/to/your/shared/folder # Optional, for photo libraries linked to Nextcloud

# Gluetun VPN Configuration
# Choose 'wireguard' or 'openvpn'
VPN_TYPE=wireguard

# Server countries list (ex: France,Germany,Netherlands)
# To see full list: docker run --rm qmcgaw/gluetun format-servers -protonvpn
# For Canada-based servers: United States,Canada,Netherlands (low latency)
# For Europe-based servers: Netherlands,Switzerland,Spain (P2P-friendly laws)
SERVER_COUNTRIES=United States,Canada,Netherlands

# OpenVPN Configuration (if VPN_TYPE=openvpn)
# IMPORTANT: Add "+pmp" at the end of your username for port forwarding
OPENVPN_USER=your_username+pmp
OPENVPN_PASSWORD=your_password

# Wireguard Configuration (if VPN_TYPE=wireguard)
# Get your key from: https://account.protonvpn.com/downloads
# Select: Router + NAT-PMP (Port Forwarding) + Uncheck VPN Accelerator
WIREGUARD_PRIVATE_KEY=your_wireguard_private_key